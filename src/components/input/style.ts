import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "80%",
    height: 65,
    backgroundColor: "#fff",
    borderRadius: 15,
    borderColor: "#000",
    borderWidth: 1,
    color: `#000`,
    fontFamily: "Helvetica",
    fontSize: 16,
    textAlign: "left",
    padding: 15,
    marginTop: 15,
    marginBottom: 15,
  },
});
